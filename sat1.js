// else if

const me = "deeksha nv"
let name;
if (name < 9) {
 name  = "initial not included";
} else if (name < 10) {
  name = "initial might be included";
} else {
  name = "initial is included";
}
console.log(name);

